var myStr="   functionUp    ";
var trimStr=myStr.trim();
console.log(trimStr);

var lowerStr="Focus ON WHat IS imPOrtanT";
var lowStr=lowerStr.toLocaleLowerCase();
console.log(lowStr);

var upperStr="life goes on";
var upStr=upperStr.toUpperCase();
console.log(upStr);